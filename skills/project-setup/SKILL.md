---
name: project-setup
description: Use when initializing local .omp state directories for a workspace.
---

# Project Setup

Initialize `.omp` state, artifact, session, skill, and ultragoal directories.

Python implementation: `src/skills/project_setup.py`.