---
name: ask
description: Use when routing a prompt to an external provider advisor and recording an ask artifact.
---

# Ask

Prepare, check, execute, and record provider advisor requests.

OMP CLI: `omp ask <provider> <prompt>` and `omp ask --execute <provider> <prompt>`.

Python implementation: `src/skills/provider_advisor.py`.