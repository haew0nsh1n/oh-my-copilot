---
description: "Prepare context for a manual native /compact handoff."
---

# OMP Manual Context Compaction Helper

Plugin commands cannot trigger a native `/compact` command. Preserve the note:

```text
$ARGUMENTS
```

Tell the user to run native `/compact $ARGUMENTS` directly.