# Mission

Achieve oh-my-claudecode public feature parity in oh-my-copilot while translating Claude Code specific plugin, hook, and tmux behavior into Copilot-native Python, CLI, state, artifact, task, and bridge surfaces.

## Outcomes

- Keep OMC public `skills/`, `commands/`, `agents/`, `hooks/`, `templates/`, `bridge`, `benchmark`, and `benchmarks` surfaces represented in this repository.
- Use Matt Pocock skills from `.agents/skills/` for every implementation slice.
- Maintain executable proof through focused pytest, `omp doctor --strict`, bridge smoke, and E2E runtime parity tests.